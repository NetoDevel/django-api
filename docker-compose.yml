version: '3'
services:
  angular-client:
    build: edge-app
    ports:
      - "4200:4200"
  nginx:
    build: nginx
    links:
      - angular-client
    ports:
      - "80:80"
    depends_on:
      - angular-client
